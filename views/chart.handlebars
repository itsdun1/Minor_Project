{{> hearderex}}




 <div class="container">





<div id="table-location">

</div> 
<br/>
<!-- Location where the chart will be added -->
<div id="chart-location">
  
</div>
<script id="tabular-template" type="text/html">



<span>
  <h1> welcone {{username}}</h1>
  <button class="btn btn-success btn-lg"><a href={{link}}>All Graphs</a></button>
</span>
<h2>Tabular Data</h2>
<div class="table-responsive">
<table class="table table-bordered table-hover table-dark">
  <thead>
    <tr>
      <td>Month</td>
      \{{#each categories}} <td>\{{label}}</td> \{{/each }}
    </tr>
  </thead>
  <tbody>
    \{{#each dataset}}
    <tr>
      <td>\{{seriesname}}</td>
      \{{#each data}} <td>\{{value}}</td> \{{/each}}
    </tr>
    \{{/each}}
  </tbody>
</table>
</div>
</div>
</script>
